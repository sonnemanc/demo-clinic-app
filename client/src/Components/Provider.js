import React from 'react'
// we an map through the providers and list them out here or this can be a "container" for providers and we map them elsewhere, i think here works for me!
function Provider() {
  return (
    <div>Provider</div>
  )
}

export default Provider