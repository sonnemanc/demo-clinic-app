import React from 'react'
// I was thinking here a user can book appointments  we can decide if they would also see their clients visits in this componenet or a separate one

function Schedule() {
  return (
    <div>Schedule</div>
  )
}

export default Schedule