import React from 'react'
// persist whats in the textbox so they can see what they typed but on successful login it resets 
// use onchange rather than onsubmit (saves to state as you type in)
function Login() {
  return (
    <div>Login</div>
  )
}

export default Login